# Los builds de desarrollo de este workspace llegaron a 37 GB con incremental
# y debuginfo completo. Sin incremental y con sólo tablas de líneas caben.
[build]
incremental = false

[profile.dev]
debug = "line-tables-only"
